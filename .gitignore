# Compiled executables
main
a.out

# Debugging symbols (macOS)
*.dSYM/

# VS Code settings (optional, but good practice to ignore)
.vscode/

# JSON library (optional: some people ignore this and use package managers, 
# but since it's header-only, it's fine to leave it out of the gitignore so it pushes to GitHub)